# Absolute API origin for the coach mobile app (no trailing slash).
# Local: http://localhost:8787  (or your machine LAN IP for a physical device)
# Staging: https://api.example.com
EXPO_PUBLIC_API_URL=http://localhost:8787
