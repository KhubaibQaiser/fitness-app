import type { KeyboardToolbarProps } from './keyboard-toolbar-types';

/** Web + Android: IME/return key is enough. iOS pads use `keyboard-toolbar.ios.tsx`. */
export const KeyboardToolbar = (_props: KeyboardToolbarProps) => null;
